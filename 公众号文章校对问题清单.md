# 公众号文章校对问题清单

校对日期：2026-07-23

## 校对规则

- 只核对 `src/公众号文章` 中的发布副本，原知识库正文未修改。
- 正文中的事实、版本、语法或示例错误只记录，不直接改正文。
- 标题、H1 和标题层级问题已直接修正。
- 已核对但正文有问题的文章标为“待修正”；空白或占位稿保留“待补充”；核对无误的文章移除“待校对”。

## 结果说明

- 共保留 242 篇知识稿：199 篇“待修正”、42 篇“待补充”、1 篇已校对且无待办状态。
- `src/公众号文章` 内已经没有“待校对”标记。
- “待修正”表示已经完成校对，但正文仍有下列问题，尚不适合直接发布。
- “待补充”表示没有足够正文可核对，不能视为校对通过。
- `NestJS项目实战 24｜项目总结与学习成果` 主要是课程感言，并非知识类文章，已从发布副本剔除；原知识库来源未删除。

## 01｜HTML

- 01：把“块级/行内元素”当成 HTML 内容模型，概念混淆；现代 HTML 的内容分类不能用这组旧式显示分类替代。
- 02：根相对路径解释不完整；把 `track` 当成 `video` 属性；表格示例和 `caption`、`thead`、行组说明有误；`iframe` 使用了过时属性。
- 03：出现不存在的 `<reset>` 标签；`checked`、`selected` 的含义及重复使用说明不准确；`textarea` 不支持文中所写的 `min`、`max`。
- 04：ARIA 使用过度；`div role="button"` 示例缺少键盘与焦点支持；`aria-haspopup` 的使用场景不准确。

## 02｜CSS

- 01：层叠优先级漏掉来源、`!important` 和层叠层；`initial`、`unset`、`revert` 的解释错误；`@layer` 说明不完整。
- 02：`fontfamily` 拼写错误；`@font-face` 示例缺少分号；默认字号不保证是 `16px`；`:where()`、`:is()` 权重说明错误；多个伪类缺少冒号。
- 03：`width` 的初始值是 `auto`，不是固定 `100%`；高度类比不成立；边框默认值解释错误；`border-box` 说明写反；`overflow` 初始值不是 `auto`。
- 04：绝对定位和固定定位的包含块规则过度简化；遗漏 transform 等会建立包含块的情况；`z-index:auto` 不是 `1`；sticky 与层叠上下文说明不准确。
- 05：存在 `gird-auto-flow`、`justfy-items` 拼写错误；对齐属性混用；隐式网格与 `grid-template-areas` 说明有误。
- 06：`flex-grow`、`flex-shrink` 的计算被过度简化；出现无效 `</>`；`inline-flex`、`inline-grid` 的行为解释错误；BFC 的作用被夸大。
- 07：`box-shadow` 语法以及 blur/spread 含义有误；`drop-shadow()` 的说明不准确。
- 08：transform 函数执行顺序写反；把 position 与 transform 的职责混淆；颜色与导航部分仍是空内容。

## 03｜JavaScript 基础

- 01：“用 Java 语法写 JavaScript”的说法错误；相等比较和 `NaN` 判断有误；对象字面量缺少逗号。
- 02：代码块混入未注释中文；遗漏 Symbol 键；把 `map.set()` 当成读取方法；称 Set 为“无序”不严谨；Generator 部分为空。
- 03：块级作用域说明不全；`let` 的提升与暂时性死区解释错误；函数表达式和 Date 示例有误；“所有值都是对象/JSON”错误；重复及未声明变量会导致代码失败。
- 04：原型与 `__proto__` 说明不准确；`this` 规则过度简化；把 `apply` 写成 `call`；遗漏 `bind` 的预置参数能力。
- 05：Promise 状态术语有误；ES 模块支持顶层 `await`；代码块混入未注释正文。
- 06：`getAttribute` 示例目标不存在；DocumentFragment 不保证完全没有重排；DOM 操作不一定都立即触发回流；表单部分不完整。
- 07：“Node 不支持 ESM”的表述已过时；namespace import 被错误命名。
- 08：示例调用未定义的 `__debounce`、`__throttle`，且正文残缺。
- 09：同一作用域重复声明常量和函数；代码块混入中文；顶层 `this` 的结论只适用于经典脚本，不适用于模块。

## 04｜JavaScript 常用 API

- 01：并非所有字符串方法都返回字符串或“新字符串”；`charCodeAt()` 返回 UTF-16 code unit，不是 ASCII 码。
- 02：多个数组示例错误地使用逗号运算符，所谓“连续访问”结论不成立。
- 03：`Object.keys/values/entries` 只处理自有、可枚举、字符串键；`Object.freeze()` 是浅冻结。
- 04：查询字符串拼接未做编码，遇到特殊字符会出错。
- 05：内容重复；多处方法返回值结论错误；“对象所有键”说法过宽；`Object.is()` 不能简单概括为“比严格相等更严格”。

## 05｜浏览器运行机制

- 01：把 Promise 回调归为宏任务；把 `Promise.resolve()` 本身当作任务；把 `requestAnimationFrame` 简单归为宏任务，均不准确。
- 02：回流、重绘和合成的触发关系写得过于绝对。
- 03：只有图片，没有解释 `defer`、`async` 与模块脚本的真实执行差异。

## 06｜HTTP、AJAX、Fetch 与 Axios

- 01：XHR 数据方向和 readyState 说明错误；Axios 不是简单的“Fetch/XHR 统一封装”；Fetch 示例没有检查 `response.ok`。
- 02：Axios 不会自动取消重复请求；`innerHTML` 示例有 XSS 风险；`JSON.parse` 异常没有进入预期的 Promise 拒绝流程。
- 03：封装实例未导出，示例 API 不完整。
- 04：响应拦截器已经返回 `res.data`，静态类型却仍按 `AxiosResponse`/自定义 `Promise<T>` 处理，运行值与类型不一致。

## 07｜Git

- 01：`user.password` 不是 Git 的有效身份配置；reflog 不是每次文件编辑记录；多个 reset/rm/恢复命令解释错误且可能误导删除；存在命令拼写问题。
- 02：clone URL 中有无效空格；`master`、`main` 混用；括号中的“命令”不可执行；把 pull 简化为固定行为；upstream 语法混用。

## 08｜Node.js 基础

- 01：SPA、SSR、CSR 的分类维度混在一起；部署和性能结论过度绝对；内容本身也不属于 Node.js 基础。
- 02：多个 pnpm workspace 命令被写成等价行为；store/cache、`prune`、`status` 的含义不准确；“Monorepo 第一选择”等结论缺少依据。
- 03：Express 只有占位内容，保持“待补充”。

## 09｜包管理与工程脚本

- 01：把当前仓库不存在的配置和脚本写成既成事实；`0.0.0` 不等于绝不会发布；`type:module`、Prettier 与 dependencies 的说明过度简化。
- 02：Oxlint 插件职责写错；配置互相矛盾且有缺项。
- 03：流程依赖特定版本且无法按文复现；ESLint 配置与 TypeScript 条件不一致。

## 10｜TypeScript

- 01：`tsc --init`、`Object/object/{}/unknown` 等概念说明有误；多个示例存在语法和类型错误；含失效链接。
- 02：多处代码无效；类型断言不会执行运行时校验；DOM 可空值未处理；递归与 Promise 示例不完整；Canvas 可能为 null。
- 03：类、元组、枚举、Symbol、迭代器示例中存在大量编译错误；`super`、只读元组、extends、symbol 键等说明有误。
- 04：所谓函数重载没有按 TS 重载签名实现；存在 `retrurn` 等拼写；泛型直接算术、XHR 与 API 类型示例均有错误；类型声明不能代替运行时校验。
- 05：声明文件与 tsconfig 示例有误；把 `rootDirs` 当成运行时合并；moduleResolution 内容过时；UMD/ESM/Express 示例无效。
- 06：混用旧版实验装饰器与 TypeScript 5 标准装饰器；`emitDecoratorMetadata` 能力被夸大；重复类与装饰器代码无效；缺少 VueLoaderPlugin。
- 07：发布订阅与观察者模式混淆；`CustomEvent`、`splice(-1)`、WeakMap/WeakSet、Proxy Map/Set 与 autorun 示例存在错误。
- 08：`typeof` 对函数的结果、类型谓词与重复声明有误；协变/逆变方向写反；`ReturnType` 示例语法错误。
- 09：类型递归示例含拼写、重复声明和无效类型。
- 10：插件主题正文缺失，`keyof` 示例也不完整；当前按“待修正”保留。

## 11｜ESLint、Oxlint 与 Prettier

- 01：引用了发布副本中不存在的 package/config 文件和 scripts。
- 02：Oxlint 插件的用途和配置说明错误。
- 03：Prettier 配置前后矛盾。
- 04：工作流调用了当前项目并不存在的命令，无法按文执行。

## 12｜Vite

- 01：当前 Vite 8 已使用 Rolldown，文中“开发用 esbuild、生产用 Rollup”的总括已过时；脚手架、npm、bare import 示例也有错误。
- 02：`loadEnv` 第三个参数、配置合并、mode/env 文件规则有误；Vue SFC 流程被错误描述。
- 03：`localsConvention`、`devSourcemap` 所属位置和 source map 说明有误；环境变量前缀、PostCSS 结论不准确。
- 04：多处路径/API 拼写错误；`?url`、alias、webpack `baseUrl` 及 Rolldown 说明有误。
- 05：插件 hook 说明和手写插件代码错误；使用过时插件；mock 示例残缺。
- 06：TypeScript 语言服务、`skipLibCheck` 和环境变量名称前后不一致。
- 07：性能指标、计时器、线程、16.6ms、`requestIdleCallback`、lodash、hash、CJS、动态导入与 Rolldown 的说明存在错误。
- 08：同源/CORS、代理、`loadEnv`、配置合并、ESM/CJS、插件和 `assetsInlineLimit` 说明有误。

## 13｜Tailwind CSS

- 01：含无效 class；submit 不是 form；存在未使用导入。
- 02：断点不等于具体设备；Tailwind CSS 4 的浏览器脚本是 `@tailwindcss/browser` 且仅适合开发，正文仍按旧方式描述。
- 03：混用 Tailwind CSS 3 的配置与 4 的 CSS-first 配置；dark variant、`@variant` 与 JIT 说明有误。
- 04：仍有“file/highlight/less javascript”等占位文本，内容不完整。

## 14｜Vue 3 基础

- 01：npm scripts 的查找规则解释错误；`npm init vue` 与官方 `create-vue` 混淆；`main.ts` 示例有拼写问题。
- 02：`<script setup name>` 无效；解构后错误使用 `.value`；标签以及 `toRef/toRefs` 示例有误。
- 03：deep watch 的结论过度绝对；替换 `person.car` 后的监听示例不成立；含未定义变量。
- 04：Vue 3.5 中 `withDefaults` 并未被废弃；Vue 2 的生命周期对应关系、Axios Promise 和自定义 hook 返回值有误。
- 05：Vuex 并非已被 Pinia“替代并停止使用”；`@input:` 语法无效；`v-model` 组件事件应为 `update:modelValue`；普通 script 中混入 TS。
- 06：`$refs` 只包含显式 ref；provide 可来自任意祖先，不只是父级；`s-slot` 标签无效。
- 07：`shallowRef`、`readonly`、`markRaw` 说明有误；含未定义变量；keyCode 修饰符已经移除。

## 15｜Vue 3 进阶

- 01：虚拟 DOM 不是 AST；`ref` 与 shallow API 可以共存；手写响应式与 `watchEffect` 示例无法正确工作。
- 02：`defineEmits` 定义重复且类型不匹配；默认插槽不强制要求 template；动态组件示例为空。
- 03：顶层 await 与 Suspense 的关系说明不完整；KeepAlive 对直接子组件的限制描述错误。
- 04：mitt 事件泛型不匹配；`defineComponent` 语法错误；TSX 中 ref 解包行为解释不准确。
- 05：指令生命周期遗漏 `created`；`import.meta.globEager` 已移除；构建 globals 与声明示例有误。
- 06：Prettier/Tailwind 插件说明不准确；Event Loop 过度简化；把浏览器 `nextTick` 与 Node 行为混为一谈。
- 07：px-to-vw 会误改不应转换的值；Tailwind CSS 4 不能简单按旧插件方案配置；Electron 部分为空。
- 08：`defineModel` 在 Vue 3.4 已稳定；env JSON、webpack、自定义元素示例错误；`minify:false` 不是性能优化。
- 09：正文说“三个宏”但只列两个；类型到运行时 props 校验的能力有限，文中说得过满。
- 10：示例缺少声明；`useTemplateRef` 的使用前后不一致。

## 16｜Pinia

- 01：缺少导入；`patch` 示例使用未声明的 `school`；store 文件名不是框架硬性要求。
- 02：localStorage 写入与读取的 key 不一致；使用上下文有误；getter 后有错误分号。
- 03：重复定义 store；state 写法错误；使用未声明的 `age`；重复常量导致编译失败。
- 04：`setUser` 重复；内置 `$reset()` 只适用于 Option Store；持久化插件缺少 SSR、异常和完整恢复处理。

## 17｜Vue Router

- 01：导入 News 却未注册为路由；嵌套路由对应的 `<router-view>` 放置错误。
- 02：query 字符串格式错误且未编码；重复 params 可能是数组；三种 props 配置被同时写成一个路由；组合式守卫并不替代全部全局守卫。
- 03：hash 模式不是默认模式。
- 04：未声明的任意 params 会被丢弃；命名路由可以同时带 query；path 中的 params 会保留；代码有语法错误，且五个小节仍为空。

## 18｜React

- 01：`useFormStatus` 来自 `react-dom`；设置相同 state 可能跳过渲染；map、`dangerouslySetInnerHTML` 示例无效；SWC 和模块打包说明有误。
- 02：`useReducer` 不是“解决 state 同步”的 API；`setAge` 示例结论过度绝对；有未声明变量、reducer 直接修改与类型错误。
- 03：localStorage 的 `getSnapshot` 每次创建新对象会导致订阅问题；React 19 中 `startTransition` 的“必须同步调用”说法已过时。
- 04：effect 执行时机和清理说明错误；SWR 数据处理、解构与可选链示例有误；`useLayoutEffect`/`useEffect` 对比过于绝对。
- 05：ref 作为依赖的结论过度泛化；React 19 中 `forwardRef` 已不再是新代码必需方案；Provider 并未移除；`useDebugValue` 返回值和 `useCallback` 说明有误。
- 06：Suspense 不能自动接管任意请求或图片；HOC 定义不准确。
- 07：styled-components 全局 CSS 示例含无效逗号。
- 08：NavLink 可用于布局；index/prefix 路由说明混乱；混入 Vue Router；StaticRouter/Next.js 内容不准确；React Router 模式推荐已过时。
- 09：CSS 写成无效 `animate`；相对导航说明有误；shell 代码块含反引号问题；TanStack Router 并不强制固定 routes 文件夹。
- 10：Zustand `get` 不接收 state 参数；Vue 集成不是官方核心能力；深层更新、Immer、浅比较、render 中订阅与默认相等比较说明均有误。

## 19｜Next.js

- 01：Turbopack “10 倍/700 倍”缺少可靠依据；React Compiler 需要安装并显式配置；并非 app 下每个文件夹都会形成公开路由。
- 02：Link prefetch 规则更细；静态生产渲染下 `useSearchParams` 才涉及特定 Suspense 要求；`permanentRedirect` 后代码不可达；平行路由 slot 的特殊文件名应为小写，Nav 只是普通组件。
- 03：`route.ts` 只能位于 app 路由树中，不是项目任意位置；httpOnly 解释不准确。
- 04：JSON 代码块含注释，不是合法 JSON；key 和本地文件处理有问题；AI SDK 示例需要固定版本后再验证。
- 05：Proxy 不能按文中方式任意修改响应正文；CSR/SSR/SSG 的框架分类过度绝对；水合前 HTML 仍可具有原生交互。
- 06：RSC 不是 React 19 才出现；不能简单等同“服务端渲染/客户端渲染”；Client Component 可通过 children 接收 Server Component；JS、DOM、BOM 的教学定义不准确。
- 07：开启 Cache Components 不会让“所有组件都动态化”；缓存边界和默认行为说明不准确。
- 08：动态 `await import` 示例不受支持；APNG、eager/preload、AVIF 和可变字体结论过度泛化；Script 注入位置、blocking、remote id、`onLoad` 大小写以及 client 边界说明有误。
- 09：`distDir` 是 Next 构建目录，不是静态导出目录；自定义图片 loader 忽略 width/quality；MD/MDX 配置有遗漏；执行不可信 MDX 有安全风险。
- 10：Server Functions 的范围比正文更广；列表格式错误；鉴权边界缺失。
- 11：locale 与 q 参数解释错误；`defaultLocale` 不能是数组；字典导入前后不一致；proxy 文件位置/扩展名错误；根路由不会按示例自动跳转。
- 12：“提升 50%”无依据；phase 常量疑似写错；JSON 示例含注释；`distDir`、`pageExtensions`、ETag、Sass 依赖说明有误。
- 13：搜索引擎优先级结论自相矛盾；`generateSitemaps` 不会自动生成根 sitemap 索引；`local` 拼写错误；metadata 的 openGraph 是浅合并；JSON-LD 对象与类型不匹配；Lighthouse 中没有文中所写的 INP 字段。
- 14：`cuid` 不是自增字段；`updatedAt` 说明不完整；数据库名称前后不一致并含弯引号；密码明文存储；连接单例和开发连接处理有问题。

## 20｜Web 渲染、SEO 与综合知识

- 01：SEO 定义过窄；SSG 的全称应为 Static Site Generation；“SSR = SPA + SSG”错误；TDK/keywords 的作用说明过时或过度绝对。

## 21｜NestJS 基础

- 01：passthrough 选项属于 `@Res()`，不是 `@Next()`。
- 02：DTO 全称是 Data Transfer Object；使用 `ParseIntPipe` 后 id 类型应为 number；该 Pipe 没有示例中的 transform 选项。
- 03：JSON 代码块含注释；Swagger 与 SWC 的关系以及元数据生成能力被夸大。
- 04：401、403、502 的含义有误；session 生产安全说明不足；provider 示例不完整。
- 05：Nest 内置 `enableCors()` 不要求另装第三方中间件；URL 明文携带凭据不安全；静态资源示例缺少逗号；上传缺少类型、大小和路径安全控制。
- 06：RxJS hot/cold 说明不严谨；出现无效 `mapfilter`；响应类型错误；异常过滤器只捕获 `HttpException`，并非所有异常。
- 07：当前通常不需要 `@types/uuid`；resource 说明不准确；全局 guard 示例被注释且依赖注入不完整；缺 metadata 时会抛错；从 query 读取角色不安全；`applyDecorators` 使用错误。

## 22｜数据库与 ORM

- 01：主体仍是选型占位，保持“待补充”；其中“Drizzle 没有稳定版”已不符合当前版本现状。
- 02：Prisma 7 的 `migrate dev` 不再自动运行 generate；示例重复声明变量、括号错误；String filter/between 写法无效；类型未导入；transaction 未 await；密码明文。
- 03：只有占位内容，保持“待补充”。
- 04：TypeORM 版本与能力结论过时；把 Nest 集成重试选项当作原生 DataSource 选项；Mongo `ObjectId` 错写为 number；缺少导入和类型。
- 05：NestJS 缓存示例基于旧版 `cache-manager-redis-store`；当前集成使用 `@nestjs/cache-manager` 与 Keyv 生态；认证和配置方式也已过时。

## 23｜JWT 身份认证

- 01：JWT 使用 Base64url，不是普通 Base64；代码有语法问题；Bearer 解析过松；RSA 密钥长度建议不足。
- 02：正则大小写处理有误；对 `JwtPayload` 使用 Omit 因索引签名不能得到预期限制；缺运行时校验、弱 fallback 密钥；同步/回调写法混乱。
- 03：TypeScript 类型不能替代外部输入校验；Express 中间件错误处理与 claim 限制不足。
- 04：标题写 RS256，正文却混用 HS256；Promise 包装与算法配置存在类型/运行风险。

## 24｜RBAC 权限控制

- 01：前端权限只控制界面，不是安全边界；模型定义与示例职责不完全一致。
- 02：指令只对已挂载节点生效，直接移除 DOM 不利于后续恢复；初始化有竞态；ComputedRef 类型和 Pinia 状态读取有误；缺少后端鉴权。
- 03：动态路由 add/remove 的名称、状态、children/meta 处理不一致，可能残留或重复路由。

## 25｜NestJS 项目实战

- 01：Nginx 不只支持 HTTP，也支持 TCP/UDP；配置可以优雅 reload，并非每次都必须完整重启。
- 02：MongoDB 不是“基于内存存储”的数据库；Egg/Koa 与 Express 的性能结论缺少测试前提。
- 03：IoC 示例实际混入继承和 Service Locator；`nest-cli.json` 的 `root` 配置错误。
- 04：QPS 不等于并发量；版本控制 diff 示例错误地用 `@Controller` 替换方法装饰器。
- 05：JSON 示例不可解析；未知异常统一返回 503、业务异常统一返回 HTTP 200 均不合理。
- 06：HMR 示例缺少 `app`；Swagger 生产入口没有保护；配置与密钥管理混淆。
- 07：Axios 异步请求未 await，外层 `try/catch` 捕获不到拒绝；只配 TS `paths` 不保证 Node 运行时解析。
- 08：缓存 API 已过时；飞书枚举类型与真实字符串值冲突；消息接口未鉴权。
- 09：MongoDB `ObjectId` 错写为 number；把 TypeORM 的 Nest 集成选项当作 DataSource 选项。
- 10：Redis 配置使用旧版 CacheModule 与 `cache-manager-redis-store`。
- 11：日志轮转在重命名失败后仍继续；未关闭定时器，可能丢写；敏感数据泄漏风险未处理。
- 12：OAuth 缺少 `state` 校验；刷新令牌返回值层级处理错误。
- 13：把飞书访问令牌直接放入 JWT；弱密钥、忽略过期时间；Cookie 缺少安全属性。
- 14：示例包含弱凭据；`synchronize:true` 未提示生产风险。
- 15：正文是 NestJS v9 历史升级方案；当前官方迁移基线已是 v11，依赖升级也不完整。
- 16：数据量估算、RBAC0 定义与职责分离说明不准确。
- 17：从数据库读取并执行任意代码，却未说明沙箱、签名、XSS/RCE 等风险。
- 18：把 alpha/beta/gamma 直接等同部署环境不严谨；构建回调缺鉴权和产物完整性校验。
- 19：通配路由代码不能正确处理 `*`；类型结构与数据不一致；信任 Host 头有缓存投毒风险；Nacos 不是通用缓存。
- 20：`fasle` 使配置失效；`nest start:dev` 不是正确 CLI 命令；Nest library 不能只靠 `nest build common` 直接发布。
- 21：客户端不应以 `0.0.0.0` 为目标地址；模块未导出客户端；权限 guard 返回非布尔业务数据，可能造成绕过。
- 22：404 测试错误期待 HTTP 200；E2E 文件不强制放 `test`；Fastify 更适合使用 `inject()`。
- 23：应使用 `pm2 logs`；Node 16 已不受 NestJS 11 支持；Dockerfile 未构建生产产物、使用旧运行时且以 root 运行；Compose 命令和 `version` 字段过时。
- 原 24：主要是课程感言而非知识文章，已从发布副本剔除，未纳入稿件统计。

## 26｜全栈扩展案例

- 01：`@nestjs/class-validator` 的使用依据不足或过时；JSON 示例含注释。
- 02：Three.js 当前 addons 导入路径与示例不一致；AmbientLight 对 MeshBasicMaterial 没有效果。
- 03：渲染器/相机流程不完整，导入写法过时。
- 04：EventSource 只发 GET；POST fetch 示例缺逗号；字节不等于 ASCII；分块读取不是完整 SSE 解析器；定时器可能泄漏。
- 05：`DeepSeek`、`@langchain` 导入无效；当前 LangChain.js 使用 `ChatDeepSeek`/`createAgent` 等 API；存在无效 `await (`；GET 请求带 body；chunk 结构假设错误。
- 06：支付宝 SDK 的 `notify_url` 配置名疑似应为 camelCase `notifyUrl`；回调地址用途说明错误；缺少签名验证、订单核验；生产切换不只是改网关。

## 27｜接口文档与测试

- 01：JSON 代码块含注释；把 JSON 路由与文件混淆；导入和依赖不完整。
- 02：Swagger 导入/依赖不完整，query 声明与实际参数不一致。
- 03：测试依赖类型写错；直接实例化 ConfigService 不合理；混用 Fastify `inject()` 与 Supertest；测试路径并非硬性要求；404 状态断言错误；上下文不完整。

## 28｜Docker、PM2 与 Linux 部署

- 01：示例写入明文凭据；MySQL 5.7 已结束生命周期；Java 基础镜像过旧；含 `bush` 拼写和容器名称冲突；network inspect 命令反斜杠错误。
- 02：ADD 自动解压能力被过度泛化；ENTRYPOINT 仍可用 `--entrypoint` 覆盖；`docker compose down` 会移除资源，不只是停止。
- 03：命令应为 `pm2 logs`；cluster 示例缺少 `-i`；PM2 管理的是进程，不是线程。
- 04：Linux 服务器文章只有占位内容，保持“待补充”。
- 05：网络安全与 Canvas 只有占位内容，保持“待补充”。

## 29｜Go 环境与工具链

- 01：`go env -w GOENV=...` 不可用，GOENV 只能通过操作系统环境设置；GOBIN 不只服务“第三方包”；另有拼写错误。
- 02：只有占位内容，保持“待补充”。

## 30｜Go 语言基础

- 01：项目与 module 不是严格一一对应；go 指令不是“实际工具链版本”的简单注释；导出规则、uint、无类型常量和 rune 的说明均不准确。
- 02：`make([]T,0)` 不保证分配可用底层数组，也不一定优于 nil slice；容量增长是实现细节；`copy` 不是普遍深拷贝；未使用局部变量导致示例不能编译。
- 03：跨文件 init 顺序不保证按文件名；错误处理建议过度绝对；文件未关闭；`errors.Is` 不只是值相等；示例调用未定义函数。
- 04：Go 不支持同作用域函数重载，也不能给同一类型的值/指针接收者定义两个同名方法；指针不保证固定 8 字节；GC 不消除所有滞留；`os.Exit` 会绕过 defer。
- 05：`type myint int` 是新定义类型而非别名；Go 没有 class/继承，嵌入是组合和成员提升；未导出字段是包内可见；接口不是“本质上的指针”。
- 06：接口的动态 pair 不能泛化到所有变量；终端示例不跨平台且文件未关闭；反射可能 panic；`omitempty` 与零值说明有误；不存在顶层 `json.Decode`；泛型零开销、any 装箱和固定优先级均过度绝对；约束示例过时。
- 07：用 `time.Sleep` 等待 goroutine 不可靠；WaitGroup 是完成同步，不是数据通信；GMP 图过度简化；共享数据还可用原子等同步手段。
- 08：已关闭但缓冲区未清空时，接收的 `ok` 仍为 true；并非所有 channel 都要关闭；重复 `ctx :=` 不能编译；取消说明不精确；WithValue 写入和读取 key 类型不同，结果必为 nil。
- 09：GOPATH 并未完全淘汰；IDE 不保证自动创建 go.mod；vendor/verify 说明过宽；直接手改 require 不应作为等价首选；replace 示例格式无效。
- 10：测试可使用同包或 `_test` 外部包；Test 命名规则、`t.Log`、`Parallel` 说明有误；当前 Go 更推荐 `b.Loop`；`-bench=.` 默认仍会运行普通测试。

## 31｜Gin 框架

- 01—05：五篇都是纯占位稿，没有技术正文可核对，标题结构正常，全部保持“待补充”。

## 32｜Java SE

- 01—07：原始源文件为空，七篇均为占位稿，没有技术正文可核对，全部保持“待补充”。

## 33｜Java Web

- 01—06：原始源文件为空，六篇均为占位稿，没有技术正文可核对，全部保持“待补充”。
- 后续补写时需注意：Jakarta Servlet 使用 `jakarta.servlet.*`；JDBC 标准包仍是 `java.sql` 与 `javax.sql`，不是 `jakarta.sql`。

## 34｜Spring SSM

- 01—05：原始源文件为空，五篇均为占位稿，没有技术正文可核对，全部保持“待补充”。

## 35｜Spring Boot

- 01—06：原始源文件为空，六篇均为占位稿，没有技术正文可核对，全部保持“待补充”。

## 36｜Spring Cloud

- 01—07：原始源文件为空，七篇均为占位稿，没有技术正文可核对，全部保持“待补充”。
- 后续补写时必须先固定 Spring Boot 与 Spring Cloud Release Train 的官方兼容组合，不能任意混用版本线。

## 37｜LangChain.js

- 01：`process.loadEnvFile` 不是 Node 20.6 加入，而是 v20.12.0/v21.7.0；`OPENAI_BASE_URL` 是否自动生效的结论不符合当前 openai-node；“一切皆 Runnable”过度概括。
- 02：聊天模型当前支持 `model.invoke("...")`，并非不能传裸字符串；`res.content` 可能是内容块数组；`usage_metadata` 只在提供商返回时存在。
- 03：stream 返回 chunk，粒度不保证“逐字”；第三方 OpenAI 兼容端点不保证支持相同结构化输出；`batch` 是并发调用接口，不等同提供商原生批任务。
- 04：“一切皆 Runnable”“实现模型无关”都过度绝对；统一接口不代表所有对象都是 Runnable，也不代表切换提供商无需安装包、配置和能力适配。
- 05：把 LangChain 框架、LangGraph 运行时、Deep Agents harness 与 LangSmith 的测试/评估/部署平台混为一体；使用旧版组件分类；现代 AIMessage 有结构化 tool_calls/content blocks，不是只能返回 JSON 字符串；Agent/LLM 对比过于绝对。
- 06：把 id、token 用量笼统放入 metadata；PromptTemplate 用于模板化，不负责自动“优化”；few-shot 配置不是固定五个必填参数；MessagesPlaceholder 的支持取决于 ChatPromptTemplate 消息结构，不是 invoke 本身。
- 07：用 Python 的 callable/Mapping/子类术语描述 TypeScript；同一代码块重复 import、`const chain` 与 `const result`，不能编译；`String(chunk.content)` 会错误处理结构化内容；JsonOutputParser 不保证模型一定生成合法 JSON。
- 08：同系列依赖版本 `1.3.2` 与 `1.3.7` 自相矛盾；`Annotation.Root` 已不是当前首选状态定义路径；只 invoke 一次，未真正展示跨调用记忆。
- 09：多个代码块重复声明 loader/docs/splitter/results，不能编译；Vector Store 不等同“向量数据库”；相似度检索链路中是 embedding 模型生成向量、向量存储检索，不是 LLM 负责向量相似度。

## 38｜RAG

- 01：拆分后仍声称本篇包含 Python/TypeScript Demo 与 LangChain.js 实战，实际只有概念；把 RAG 固定成 2-step RAG；Embedding/向量库并非所有最小 RAG 的必需项；“模型不负责搜索”不适用于 Agentic RAG。
- 02：“零依赖”不准确，TypeScript 命令依赖 `ts-node` 并假定 CommonJS 的 `__dirname`；示例没有生成模型，只是检索后规则拼接；缺少运行环境、目录和版本前提。
- 03：聊天与 Embedding 共用同一个 `OPENAI_BASE_URL`，若地址是 DeepSeek 官方 API，默认 OpenAI Embedding 无法调用；默认 `deepseek-chat` 将于 2026-07-24 停用；持久化向量库迁移和生产安全被过度简化；缺依赖、Node.js 与环境变量说明。
- 04：再次把所有 RAG 固定成“文档→Embedding→向量库→检索→生成”；“检索决定上限”“漏召回一定更严重”均过于绝对。

## 39｜Agent 与 MCP

- 01：拆分后仍承诺包含代码、记忆和 Agentic RAG，实际只讲概念；MCP 被简化成模型连接工具的标准，遗漏 Host/Client/Server、Resources、Prompts、Sampling、Elicitation、Logging 等；协议不规定应用如何使用 LLM；“无工具 Agent 只能说”过于绝对。
- 02：工具不是所有最小 Agent 定义的必需项，LangChain `createAgent` 可接收空工具列表；把任意判断/行动/观察循环都叫 ReAct；遗漏最小权限、提示注入、审批、沙箱与费用/循环上限。
- 03：当前 LangChain.js v1 使用 `createAgent({ model, tools, systemPrompt })`，正文却写成 `{ llm, tools, prompt }` 并把新旧参数说反，示例无法运行；`getType()` 已弃用；默认模型即将停用；把 Agent 路径写成确定顺序。
- 04：三段示例继续使用错误参数；`checkpointer + thread_id` 只是线程级短期记忆，长期记忆需要 Store；聊天与 Embedding 的 DeepSeek 地址组合通常不可用；固定可猜的 thread_id 有越权风险；默认模型即将停用。

## 40｜Codex

- 01：对照 2026-07-23 的 OpenAI 官方命令表，所列 CLI 命令及核心解释未发现明确事实错误；已移除“待校对”。
- 02：IDE 现行命令是 `/ide-context`，不是正文中的 `/auto-context`；App/IDE 命令会随环境与权限变化。正文未改，已标为“待修正”。

## 41｜开发环境与工作流

- 01：Tailwind CSS 4 配置缺少 CSS 中的 `@import "tailwindcss"`；示例把 Vite plugins 重写为只有 Tailwind 插件，会丢掉 Vue 插件；“element ui”应为 Element Plus。Element Plus 全量导入及 Volar 类型片段本身可成立。
- 02：`Vetony` 应为 Ventoy，且“极安全”没有依据，首次安装默认会格式化 U 盘；BIOS/UEFI 固件不是驱动；全局放开 node_cache 权限不属于标准要求且有安全风险；Docker Desktop 需要核对 WSL/系统/虚拟化前提；VMware 的 ESD/“BISO”说明有误；Go 内链失效；多个软件名拼错；Windows Terminal 的 `acrylicOpacity` 已是旧属性，现代配置使用 `opacity` 与 `useAcrylic`，且属性应放在 `profiles.defaults`。

## 主要核对依据

- Web 平台：[MDN Web Docs](https://developer.mozilla.org/)、[HTML Living Standard](https://html.spec.whatwg.org/)
- JavaScript/TypeScript：[ECMAScript](https://tc39.es/ecma262/)、[TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)
- 工程化：[Vite](https://vite.dev/guide/)、[Tailwind CSS](https://tailwindcss.com/docs/installation/using-vite)、[pnpm](https://pnpm.io/)
- Vue 生态：[Vue](https://vuejs.org/guide/introduction.html)、[Pinia](https://pinia.vuejs.org/)、[Vue Router](https://router.vuejs.org/)
- React/Next.js：[React](https://react.dev/)、[React Router](https://reactrouter.com/)、[Next.js](https://nextjs.org/docs)
- NestJS/数据层：[NestJS](https://docs.nestjs.com/)、[Prisma](https://www.prisma.io/docs)、[TypeORM](https://typeorm.io/)
- 容器与 Go：[Docker](https://docs.docker.com/)、[Go](https://go.dev/doc/)
- Java/Spring：[Java SE](https://docs.oracle.com/en/java/javase/)、[Jakarta Servlet](https://jakarta.ee/specifications/servlet/)、[Spring](https://docs.spring.io/)
- AI：[LangChain.js](https://docs.langchain.com/oss/javascript/langchain/overview)、[Model Context Protocol](https://modelcontextprotocol.io/docs/getting-started/intro)
- Codex：[OpenAI Codex developer commands](https://developers.openai.com/codex/cli/slash-commands)
- Windows 环境：[Windows Terminal](https://learn.microsoft.com/windows/terminal/customize-settings/profile-appearance)、[Docker Desktop for Windows](https://docs.docker.com/desktop/setup/install/windows-install/)、[Ventoy](https://www.ventoy.net/en/doc_start.html)
